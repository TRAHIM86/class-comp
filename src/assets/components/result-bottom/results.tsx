import { result_sec } from '../../../styles/styles';
import { TableResult } from './tableresult';

export const Result = () => {
  return (
    <section className={result_sec}>
      <TableResult />
    </section>
  );
};
